import {Item} from '../../item';
import {COPPER_INGOT} from '../../../util/names';

export class CopperIngot extends Item {
  name = COPPER_INGOT;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
