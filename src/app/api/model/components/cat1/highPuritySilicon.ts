import {Item} from '../../item';
import {HIGH_PURITY_SILICON} from '../../../util/names';

export class HighPuritySilicon extends Item {
  name = HIGH_PURITY_SILICON;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
