import {Item} from '../../item';
import {TITANIUM_INGOT} from '../../../util/names';

export class TitaniumIngot extends Item {
  name = TITANIUM_INGOT;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
