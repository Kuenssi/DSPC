import {Item} from '../../item';
import {PHOTON_COMBINER} from '../../../util/names';

export class PhotonCombiner extends Item {
  name = PHOTON_COMBINER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
