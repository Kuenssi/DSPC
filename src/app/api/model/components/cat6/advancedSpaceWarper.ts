import {Item} from '../../item';
import {A_SPACE_WARPER} from '../../../util/names';

export class AdvancedSpaceWarper extends Item {
  name = A_SPACE_WARPER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
