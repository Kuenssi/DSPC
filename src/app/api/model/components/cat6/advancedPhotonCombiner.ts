import {Item} from '../../item';
import {A_PHOTON_COMBINER} from '../../../util/names';

export class AdvancedPhotonCombiner extends Item {
  name = A_PHOTON_COMBINER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
