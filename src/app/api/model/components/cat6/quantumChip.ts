import {Item} from '../../item';
import {QUANTUM_CHIP} from '../../../util/names';

export class QuantumChip extends Item {
  name = QUANTUM_CHIP;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
