import {Item} from '../../item';
import {A_PARTICLE_CONTAINER} from '../../../util/names';

export class AdvancedParticleContainer extends Item {
  name = A_PARTICLE_CONTAINER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
