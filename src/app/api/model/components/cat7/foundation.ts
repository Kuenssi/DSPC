import {Item} from '../../item';
import {FOUNDATION} from '../../../util/names';

export class Foundation extends Item {
  name = FOUNDATION;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
