import {Item} from '../../item';
import {ENERGY_MATRIX} from '../../../util/names';

export class EnergyMatrix extends Item {
  name = ENERGY_MATRIX;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
