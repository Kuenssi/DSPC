import {Item} from '../../item';
import {INFORMATION_MATRIX} from '../../../util/names';

export class InformationMatrix extends Item {
  name = INFORMATION_MATRIX;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
