import {Item} from '../../item';
import {GRAVITY_MATRIX} from '../../../util/names';

export class GravityMatrix extends Item {
  name = GRAVITY_MATRIX;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
