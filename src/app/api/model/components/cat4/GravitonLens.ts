import {Item} from '../../item';
import {GRAVITON_LENS} from '../../../util/names';

export class GravitonLens extends Item {
  name = GRAVITON_LENS;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
