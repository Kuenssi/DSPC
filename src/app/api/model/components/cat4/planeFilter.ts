import {Item} from '../../item';
import {PLANE_FILTER} from '../../../util/names';

export class PlaneFilter extends Item {
  name = PLANE_FILTER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
