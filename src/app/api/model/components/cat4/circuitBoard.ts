import {Item} from '../../item';
import {CIRCUIT_BOARD} from '../../../util/names';

export class CircuitBoard extends Item {
  name = CIRCUIT_BOARD;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
