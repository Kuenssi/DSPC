import {Item} from '../../item';
import {CARBON_NANOTUBE} from '../../../util/names';

export class CarbonNanotube extends Item {
  name = CARBON_NANOTUBE;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
