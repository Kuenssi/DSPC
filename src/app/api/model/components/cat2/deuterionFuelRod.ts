import {Item} from '../../item';
import {DEUTERION_FUEL_ROD} from '../../../util/names';

export class DeuterionFuelRod extends Item {
  name = DEUTERION_FUEL_ROD;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
