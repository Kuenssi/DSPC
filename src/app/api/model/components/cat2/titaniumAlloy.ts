import {Item} from '../../item';
import {TITANIUM_ALLOY} from '../../../util/names';

export class TitaniumAlloy extends Item {
  name = TITANIUM_ALLOY;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
