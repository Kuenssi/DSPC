import {Item} from '../../item';
import {MAGNETIC_COIL} from '../../../util/names';

export class MagneticCoil extends Item {
  name = MAGNETIC_COIL;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
