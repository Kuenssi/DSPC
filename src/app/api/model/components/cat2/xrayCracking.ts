import {Item} from '../../item';
import {X_RAY_CRACKING} from '../../../util/names';

export class XrayCracking extends Item {
  name = X_RAY_CRACKING;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
