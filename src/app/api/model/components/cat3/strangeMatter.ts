import {Item} from '../../item';
import {STRANGE_MATTER} from '../../../util/names';

export class StrangeMatter extends Item {
  name = STRANGE_MATTER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
