import {Item} from '../../item';
import {TITANIUM_CRYSTAL} from '../../../util/names';

export class TitaniumCrystal extends Item {
  name = TITANIUM_CRYSTAL;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
