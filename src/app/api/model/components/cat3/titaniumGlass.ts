import {Item} from '../../item';
import {TITANIUM_GLASS} from '../../../util/names';

export class TitaniumGlass extends Item {
  name = TITANIUM_GLASS;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
