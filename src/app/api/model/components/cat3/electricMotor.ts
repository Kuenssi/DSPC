import {Item} from '../../item';
import {ELECTRIC_MOTOR} from '../../../util/names';

export class ElectricMotor extends Item {
  name = ELECTRIC_MOTOR;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
