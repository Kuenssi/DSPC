import {Item} from '../../item';
import {PLASMA_EXCITER} from '../../../util/names';

export class PlasmaExciter extends Item {
  name = PLASMA_EXCITER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
