import {Item} from '../../item';
import {SPACE_WARPER} from '../../../util/names';

export class SpaceWarper extends Item {
  name = SPACE_WARPER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
