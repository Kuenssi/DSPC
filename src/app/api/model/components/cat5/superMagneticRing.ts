import {Item} from '../../item';
import {SUPER_MAGNETIC_RING} from '../../../util/names';

export class SuperMagneticRing extends Item {
  name = SUPER_MAGNETIC_RING;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
