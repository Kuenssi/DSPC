import {Item} from '../../item';
import {CASIMIR_CRYSTAL} from '../../../util/names';

export class CasimirCrystal extends Item {
  name = CASIMIR_CRYSTAL;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
