import {Item} from '../../item';
import {PARTICLE_CONTAINER} from '../../../util/names';

export class ParticleContainer extends Item {
  name = PARTICLE_CONTAINER;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
