import {Item} from '../../item';
import {PARTICLE_BROADBAND} from '../../../util/names';

export class ParticleBroadband extends Item {
  name = PARTICLE_BROADBAND;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
