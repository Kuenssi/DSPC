import {Item} from '../../item';
import {FRAME_MATERIAL} from '../../../util/names';

export class FrameMaterial extends Item {
  name = FRAME_MATERIAL;
  processingTime = ;
  neededMachine = ;
  inputs = [];
}
